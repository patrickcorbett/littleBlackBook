# profiles
spring.profiles.active=${littleBlackBook.db.vendor}

# Controllers: Dispatcher Servlet URL Pattern
server.servlet.contextPath=/rest

# Hibernate Properties
hibernate.hbm2ddl.auto = create
hibernate.show_sql = true